<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".ScheduleFragment">

    <!-- Hello User dan Tanggal -->
    <TextView
        android:id="@+id/tv_hello_user_schedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textStyle="bold"
        tools:text="Hello, User" />

    <TextView
        android:id="@+id/tv_current_date_schedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_hello_user_schedule"
        android:layout_marginTop="4dp"
        android:textSize="16sp"
        tools:text="Senin, 20 Oktober" />

    <!-- Konten yang sudah ada -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:src="@drawable/calendar"
            app:tint="?android:attr/textColorSecondary"
            android:contentDescription="Ikon Jadwal"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="No schedule found"
            android:textSize="18sp" />

    </LinearLayout>

</RelativeLayout>
